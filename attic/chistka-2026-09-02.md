# Вычистка 2 сентября 2026

Разовый заход не на новое, а на снятие мёртвого. Что убрано и как вернуть.

## Функции без единого вызова — 15 штук, ≈190 строк

`watchAdd`, `weekSummary`, `archiveUI`, `deleteEntry`, `switchTrack`,
`addPhotoTake`, `barMap`, `weekDots`, `unitWord`, `historyHTML`,
`openTalkSheet`, `viewOf`, `блокHTML`, `lcClock`, `musOf`.

Остатки переделанных экранов: недельная сводка, история занятий, добавление
видео по ссылке, отдельная шторка разбора. Ни на одну из них не вело ничего —
ни из кода, ни из разметки. `lcClock` и `musOf` жили только в тестах, тесты
сняты вместе с ними.

## Трекинг по этапам — ≈150 строк

`stages`, `byStages`, `stageKey`, `stageDoneAt`, `stagesDone`, `stageNow`,
`stageSheetUI`, `bindStageSheet`, `markStage`, `stagesPageHTML` и все ветки
`byStages()` в статистике, подписи на главной, шторке отметки и на странице
курса. Механика прожила один день: 44 хода → 14 этапов → просто «рисовал
сегодня» плюс снимок. Сами этапы лежат в `attic-argos-steps.json` каталога.

## Мёртвый CSS — 236 правил, 543 строки

Классы, которых нет ни в коде, ни в разметке: `.gz-*` (шкала целей), `.mp-*`
(карта мест старого вида), `.lv-*` (уровни), `.pile-*`, `.wallet`, `.radar`,
`.mgrid`, `.wk-*`, `.wt-*` (старый список видео), `.ls-step*`, `.dice-*`,
`.th-form`, `.material-panel` и другие. Плюс `.sg-*` от этапов, `.ai-row`
и `.ask-pre` от переехавших кнопок.

## Как вернуть

Полные тексты снятого — в scratchpad-файлах этого захода: `dead-js.md` и
`dead-css.txt`. Если их уже нет, всё лежит в истории: `git show 789c8dc:app.js`
и `git show 789c8dc:index.html` — это версия 377, последняя перед вычисткой.

## Второй заход: разбор и вопросы — ≈370 строк

Сначала я решил их оставить, сославшись на то, что у «Столпов моря»,
«Униженных» и «В лесах Сибири» файлы разбора лежат и работают. Проверил —
неправда: у этих трёх в `article-<id>.json` только карты (48, 20 и 21 место),
а `article`, `faq` и `notes` пустые. Разбор существовал ровно в одной книге, в
«Одиссее», и был там выключен.

Снято: весь блок «Разборы песней» (`openClub`, `рисуйКлуб`, `рисуйРазговор`,
`clubState`, `clubLead`, `clubSeen`, `clubMark`, `talkAt`, `talkView`),
`bookArticle`, `bookFaq`, `faqOfChapter`, `articleOfChapter`,
`articleChapters`, кнопка «Разбор» под обложкой и вся логика выбора между ней
и картой, отметки `club`/`clubAt` в профиле и их слияние при синхронизации,
26 правил CSS.

Осталась одна кнопка — 🗺 «Карта мест», и она проще: карта есть — кнопка есть.

Содержимое разбора «Одиссеи» (217 блоков, 240 вопросов, 64 примечания, 106 КБ)
уехало в `attic-odyssey-article.json` каталога. В `article-odyssey.json`
осталась только карта.

## Что НЕ трогали

- **Движок уроков** (≈540 строк): `openLesson`, `lessonRender`, `lessonNext`,
  `pastelSheetUI`, `courseTime`, `courseWatch`. Курс «Пастель» в архиве, но
  может вернуться, и это единственный способ вести курс по видео.

## Третий заход: пауза, темы, лишние настройки — ≈340 строк

- **Пауза** (53 строки + данные): раздел настроек, `freezeUI`, `bindFreezeUI`,
  `isFrozen`, `activeFreeze`, поле `freezes` в профиле, отметка «этот день в
  паузе» в календаре, вычет пауз из оценки ритма, плашка «Пауза до … серия
  сохранится» на главной. Пауза существовала ради серии, а серий в приложении
  больше нет — защищать стало нечего.
- **Оформление** (219 строк): `THEMES` (шесть тем со своими палитрами, словами
  и значками), `themeById`, `applyTheme`, `themeUI`, `bindThemeUI`, поле
  `shop.theme` и его слияние. Слова и значки у приложения теперь одни:
  `T()` всегда берёт `WORDS_BASE`, `ICON()` — переданное значение по умолчанию.
  Раздел настроек остался, но теперь это «🎧 Атмосфера» — звук и мысль дня.
- **Автокопия и каталог** в разделе «Данные» (109 строк): `archiveBackupUI`,
  `catalogUI` и их привязки. Обе делали одно и то же по расписанию сами,
  а кнопки только путали. В «Данных» осталось ровно две кнопки: сохранить
  копию и восстановить из копии.
- **CSS**: 14 правил.

Разделов настроек стало шесть вместо восьми: профиль, синхронизация, цель,
атмосфера, библиотека, данные, о приложении.

## Четвёртый заход: по счётчикам заходов — ≈520 строк

Решали не на глаз: в приложении есть `data.usage` — сколько раз открывали
каждый раздел. За месяц у обоих профилей.

- **Спрятанная игра в «Какуле»** (244 строки): зажал кнопку — полетел, свой
  рекорд в localStorage. Сама «Какуля» осталась: у Дианы 36 записей и 10
  заходов, это живой раздел. Ушла только игра.
- **Стена картин** (85 строк + 7 правил CSS): вкладка «Галерея» в артефактах,
  картины в рамах во всю ширину, `wallItems`, `картинаHTML`, `РАМЫ`, поле
  `wall` в профиле. Три захода у Антона, ноль у Дианы — избранного хватает.
- **Палитра пастели** (94 строки + 59 цветов в профиле): подставляла номера
  мелков в текст шага — «фон голубым (246)». Курсов с шагами не осталось,
  подставлять некуда. Это `пал`, `цветLab`, `мелкиДля`, `сНомерами`.
- **Поле `pills`**: пустой список и переадресация несуществующей вкладки.
- **CSS**: 34 правила.

### Две развилки сведены к общему правилу

`resetMaterial` и список курсов в библиотеке считали наследный ключ курса
вручную — каждый по-своему. Пока совпадало, но именно на таком расхождении
«Аргус» дважды пропадал с главной за день. Теперь оба зовут `keyOfCourse`
и `pracCourseKey`.

### Что проверили и не тронули

- **«Какуля»** — живой раздел Дианы.
- **`goalAt`** — выглядит мёртвым (ноль), но по нему решается, чья недельная
  цель победит при синхронизации. Убрать — и один телефон молча затрёт цель
  другого.
- **`kanyeAt`** — пасхалка в наградах «Какули», не мусор.

